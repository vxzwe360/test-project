Building the API

run build.bat on the project folder, this should build and create a docker image that can be deployed

API Config

find configs  in src/main/resources/application.properties

API documentation
swagger.doc or when the service is running can be found on the below URL

http://<host:post>/experian/studentSvc/ui.html
